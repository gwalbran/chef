date {
  match => [ "timestamp" , "ISO8601" ]
}

grok {
  match => [
    "message", "%{TIMESTAMP_ISO8601:timestamp} %{LOGLEVEL:level}%{SPACE}\[%{NOTSPACE:thread_id}\]%{SPACE}\[%{DATA:user}\]%{SPACE}%{NOTSPACE:category}%{SPACE}-%{SPACE}%{GREEDYDATA:log_msg}"
  ]
  break_on_match => false
}
